# For local development only (npm run dev). For Pi/Docker deployment use sync-server/deploy/.env.example instead.
HOST=0.0.0.0
PORT=8787
DATABASE_URL=postgres://securepass:change-me@localhost:5432/securepass_sync
JWT_ACCESS_SECRET=CHANGE_THIS_TO_A_LONG_RANDOM_SECRET_MIN_32_CHARS
JWT_REFRESH_SECRET=CHANGE_THIS_TO_ANOTHER_LONG_RANDOM_SECRET_MIN_32_CHARS
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=30d
BOOTSTRAP_TOKEN=CHANGE_THIS_TO_A_LONG_RANDOM_BOOTSTRAP_TOKEN
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=100
# Stricter limit for auth endpoints (login, register, refresh) - 20 attempts per 15 min per IP
AUTH_RATE_LIMIT_WINDOW_MS=900000
AUTH_RATE_LIMIT_MAX=20
# Set to false to block all new registrations entirely.
# When true, /register requires a valid per-user invite token (created via POST /v1/invite/create).
ALLOW_REGISTRATION=true
# Set to true when running behind a reverse proxy or ngrok so rate limiting uses the real client IP
TRUST_PROXY=false
